"""
MAC/WIN-ROUTER-CONTEXT-RUNTIME-ADVISORY-STUB-1

Advisory-only, non-production context decision stub.
Emits contract-valid model_route + context_route + receipt_consumption objects.
Offline/sidecar only — no HTTP, no router interaction, no process control.

Contract invariants: advisory == true, production_effects_allowed == false
"""

__version__ = "0.1.0"
__contract_version__ = "0.1"
